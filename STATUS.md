## STATUS.md — обновлено 28.08.2026, сессия S4

### Закрыто
- Шаблон карточки: api/templating.py + api/templates/card.html.
  Вход — только RecommendationOut, StrictUndefined, стили отдельным
  макросом (один <style> на страницу, не на карточку).
- Решение по in_stock: вариант A — шаблон о нём не знает. Недоступный
  кандидат отсеян в matching/engine.py:50 (ADR-019), поля in_stock нет
  ни в RecommendationOut, ни в PurchaseOut. availability=InStock
  безусловен и не лжёт. Вариант "серая кнопка" отклонён (отмена ADR-019
  + три слоя), "замена оффера" невозможна: продуктов с >1 оффером 0 из 21.
- Schema.org: простой товар — Product+Offer; кит — ItemList из двух
  Product со своими Offer (ADR-035).
- image_url=None — плейсхолдер с инициалами бренда, itemprop image
  отсутствует.
- Порядок блоков переделан после просмотра в браузере: что это →
  сколько протянет → цена → почему два товара → кнопки → экономика →
  оговорки под <details>. Раньше карточка начиналась с числа часов
  без контекста, а флаг про паспортную ёмкость липнул к названию
  инвертора и читался как его свойство.
- Кнопки кита подписаны ролью: "Купити інвертор" / "Купити акумулятор".
- ОКУПАЕМОСТЬ: найдена ложная строка. payback_energy_kwh (4228) больше
  lifetime_energy_kwh (2937) у всех пяти позиций — решение не окупается
  никогда. Добавлена pays_back_within_life(): окупаемость показывается
  только когда она в пределах ресурса, иначе честный текст "проти мережі
  не окупиться". Обе ветки покрыты тестами.
- jinja2>=3.1 в dependencies, "jinja2" в API_ALLOWED (tests/test_architecture.py).
- scripts/preview_card.py — превью в var/preview.html, тариф обязателен
  без дефолта (ADR-009). GET / не заводился: это S5.
- Тесты: 168 passed, 3 failed (те же известные), было 144. mypy Success
  на 64 файлах, ruff по файлам сессии — All checks passed.
  Проверено в терминале на машине владельца.

### Найдено по ходу, вне слоя S4
- catalog/components_loader.py НЕ читает accepts_solar_input и
  capacity_source из YAML. solar_optional никогда не True (0 из 37),
  capacity_source None у всех 37. Блоки в шаблоне написаны и покрыты
  тестами, но мертвы до правки загрузчика. Слой catalog/.
- Прошлая запись "capacity_source у всех повербанков RATED" неверна:
  ключа нет ни в products.yaml, ни в components.yaml.
- in_stock: 48 кандидатов, видимо движку 30. Три недоступных компонента
  убивают 11 китов из 27. Вход в S8.
- Choetech CJ2600Q-V2 заведён как инвертор — по названию сомнительно,
  проверить запись в components.yaml (S8).
- Выдача на профиле "холодильник + роутер, 4 год" состоит из пяти китов,
  ни одной станции. Вопрос ранжирования, слой matching/, нужен ADR.
- api/templates/ не объявлен package-data — важно для сборки образа (S7).
- Перелинковка / ↔ /rozrakhunok не сделана: карточка не страница,
  целевого URL нет до S10.

### Первое, что проверить в следующей сессии
- python scripts/status.py
- S5 (форма на корне) разблокирована: base.html, GET /, canonical,
  список приборов над выдачей, H1 под формулировку проблемы.