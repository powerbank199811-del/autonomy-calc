## STATUS.md — обновлено 25.08.2026, сессия S1

### Закрыто
- ADR-038 написан и зафиксирован в README.md (grep -c '^## ADR-' → должно
  быть 38, проверить после вставки). ADR_INDEX.md — вне git, это файл
  только в Claude-проекте, в репозитории не создаётся.
- Контракт: RecommendationOut.purchases: tuple[PurchaseOut, ...],
  capacity_source, solar_optional. Display-склейка — api/display_index.py,
  НЕ расширение Candidate/Recommendation (matching/ не трогается).
- Решено: изображения — own-hosted (не hotlink), задача данных S8,
  не блокер этого контракта.

### Не сделано в этой сессии (сознательно, вне слоя)
- Код не писался (правило сессии: только ADR).
- pytest НЕ запускался в S1 — три теста, известные с S0, всё ещё
  непроверенное состояние, не обновлялось.
- Claude Code установлен и настроен на этой сессии (CLAUDE.md через /init),
  но не использовался для кода — S1 велась в чате Claude.ai.

### Первое, что проверить в следующей сессии (S3 — реализация)
- Запустить pytest -q, зафиксировать актуальный список падающих тестов
  ДО начала реализации ADR-038, чтобы не путать новые падения со старыми.
- Проверить grep -c '^## ADR-' README.md == 38 перед тем как писать код
  по контракту.
- S3 — следующая по SESSIONS.md, зависит от S1 (сделано). Слой: core/ + api/.
  matching/ не трогать.
