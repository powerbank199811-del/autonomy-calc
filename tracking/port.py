"""Порт хранилища кликов.

Смысл протокола не в абстракции ради абстракции, а в конкретном сроке:
в фазе 4 появятся постбеки от CPA-сетей и подписки на снижение цены —
тогда SQLite меняется на PostgreSQL заменой одного класса, а обработчик
/go и тесты не трогаются.
"""

from typing import Protocol

from tracking.click import Click


class ClickLog(Protocol):
    """Хранилище кликов. Только запись и чтение — без удаления.

    Клик — свершившийся факт, а не запись, которую можно поправить.
    Отсутствие update/delete в порту — не забывчивость, а инвариант:
    журнал атрибуции, который можно редактировать, бесполезен как
    доказательство при споре с CPA-сетью.
    """

    def record(self, click: Click) -> None:
        """Сохраняет клик. Бросает исключение, если сохранить не удалось."""
        ...

    def count(self) -> int:
        """Сколько кликов записано. Нужно для метрики CTR фазы 1."""
        ...
